# Same syntax as .gitignore. Keeps the graph about hand-written code.

# Python virtual environment
python/.venv/

# Generated protobuf output (regenerated by `make proto` / `buf generate`)
go/gen/
python/src/harbormaster/v1/

# graphify's own build output
graphify-out/

# Caches
**/__pycache__/
python/.mypy_cache/
python/.ruff_cache/
.pytest_cache/
python/.pytest_cache/

# Node (none expected, kept for parity with skill defaults)
node_modules/
